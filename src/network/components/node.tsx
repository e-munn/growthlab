import { Box, Stack } from '@mui/material'
import _, { set } from 'lodash'
import { useState } from 'react'
import { motion } from 'framer-motion'
export const Network = ({}) => {
  return <motion.circle r={3}></motion.circle>
}
